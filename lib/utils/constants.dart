import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF6F35A5);
const Color accentColor = Color(0xFFF1E6FF);

// Defina mais constantes de cores, tamanhos de fontes, e strings reutilizáveis.
